[Unit]
Description=XBMC Media Center
After=xbmc.target

[Service]
Environment=DISPLAY=:0.0 SDL_MOUSE_RELATIVE=0 HOME=/storage
Environment=LIRCDEV=/run/lirc/lircd
ExecStart=/usr/lib/xbmc/xbmc.bin --standalone -fs --lircdev $LIRCDEV
Restart=always

[Install]
WantedBy=xbmc.target
